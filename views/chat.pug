extends layouts/main-layout.pug
block content
    article.index
        ul#messages
        form(action="")
            input#name(type="hidden" value=name)
            input#m(autocomplete="off" type="text")
            button Send